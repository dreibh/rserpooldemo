#!/bin/bash

. ./RSerPoolDemo.config


CC_PROGRAM="registrar"
CC_LOGARGS="-logfile="   # The file name will be appended immediately!

CC_ARGS="-loglevel=5 -logcolor=off \
-asap=auto -asapannounce=auto -enrp=auto -enrpmulticast=auto \
-maxbadpereports=99999999 \
$RSERPOOLDEMO_CSPOPT"
