#!/bin/sh

set -e

case "$1" in
   configure)
      cp /etc/rserpooldemo/grub-defaults /etc/default/grub
      if [ -x /usr/sbin/update-grub ] ; then
         /usr/sbin/update-grub || true
      fi
      ;;
esac

#DEBHELPER#

exit 0
